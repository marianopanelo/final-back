<h1>gracias por elegirnos , nuestros productos son :</h1>

{{#each productosAMostrar}}
    <div>
        <h2>nombre : {{title}}</h2>
        <img src="{{imagen}}" alt="{{title}}">
        <h2>Precio: {{precio}}</h2>
        <h2>stock : {{stock}}</h2>
        <h2>id : {{id}}</h2>
    </div>

<button class="boton-comprar" id="{{id}}">comprar producto </button>
<br><br>
{{/each}}

<button id="botonCarrito">ir al carrito</button>
<button id="finalizarCompra">para empezar a finalizar compra</button>
<script src="/js/productos.js"></script>
